# Codebase Memory MCP — Stack Evaluation

Date: 2026-09-16

## Upstream
https://github.com/DeusData/codebase-memory-mcp

## Role
Persistent codebase knowledge and MCP-assisted repository memory.

## Acceptance
Canonical repository inspected. Python source passed the lightweight static
acceptance gate with no reported failure signature. Review checkout remained
clean.

No MCP server was attached to a production workspace and no external
credentials were supplied.

## Decision
**PUSH — CORE-CANDIDATE CODEBASE MEMORY SPECIALIST**

Distinct enough from ordinary coding agents to retain. Core runtime promotion
still requires a contained real-repository retrieval/accuracy benchmark,
resource measurement and rollback path.
