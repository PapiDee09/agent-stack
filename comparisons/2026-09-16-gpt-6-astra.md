# GPT-6 Astra — Runtime Metadata Evaluation

Date: 2026-09-16

## Classification

Provider model capability / runtime metadata.

This is not an OSS repository dependency and should not be treated as one.

## Intended Stack Role

GPT-6 Astra is retained as model-routing metadata for workloads where its
documented context, reasoning and agentic capabilities make it an appropriate
provider option.

Relevant stack uses may include:

- large-context repository analysis
- complex coding and refactoring
- agent orchestration
- long technical documents
- high-complexity reasoning
- tool-using workflows

## Dependency Model

Vendor-hosted capability.

The reusable builder stack should reference the model through the provider
abstraction rather than couple application architecture directly to it.

No provider credential belongs in the control repository.

## Acceptance Boundary

This intake did not perform:

- API authentication
- paid inference
- credential creation
- production routing
- application migration

No runtime benchmark is claimed by this artifact.

## Decision

**REFERENCE — MODEL / RUNTIME METADATA**

Retain GPT-6 Astra in the provider/model capability catalog.

Do not classify it as an OSS stack dependency.

Any future default-model promotion should be based on reproducible workload
benchmarks covering quality, latency, cost, tool use and reliability.
